"""
API Router configuration for {{ project_name }} project.

Examples:
    from app import router1, router2
    routers = [router1, router2]
"""
from fastapi import APIRouter

routers: list[APIRouter] = []
